{
  "name": "qa-test-case-generator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "QA test case generator with Anthropic API proxy on Vercel",
  "scripts": {
    "dev": "node --env-file=.env.local local-server.js",
    "deploy": "vercel --prod"
  }
}
